# The deployment section should be excluded until
# it is explicitly included in module 5
#user later with dicker
#deployment:
#  dockerhub:
#    branch: master
#    commands:
#      - $DOCKER_HUB_TRIGGER

deployment:
  staging:
    branch: master
    heroku:
      appname: pluralsight-docker
test:
  override:    
    - mocha